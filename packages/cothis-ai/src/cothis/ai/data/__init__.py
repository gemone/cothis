"""Bundled data files for cothis (model metadata, etc.).

Read files in this package via ``importlib.resources.files("cothis.data")`` so
the lookup works the same in a source checkout and in an installed wheel.
"""
